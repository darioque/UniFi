<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head') %>
    <link rel="stylesheet" href="/css/styles-login-register.css" />
    <script src="/js/metamaskLogin.js" type="text/javascript"></script>
  </head>
<body class="bg-dark-gray">
    <header>
        <nav class="header-navbar">
            <div class="logo">
                <a href="/"><img class="header-img" src="/img/logo.svg" alt="Logo de UniFi" /></a>
            </div>
        </nav>
    </header>
    <main class="main-container color-white">
        <div class="main-text">
            <h1>Reset your password</h1>
        </div>
        <!-- Si se enviaron mensajes de error a la vista de login, mostrarlos -->
        <% if (locals && locals.errorMessages) { %> 
            <% for(let error in errorMessages) { %>
            <p class="errors"><%= errorMessages[error].msg %> </p>
            <% } %> 
        <% } %> 
        <div class="login-form-container">
            <form class="login-form" action="/login/reset-password" method="POST">
                <label for="email">Email or Username</label>
                <div class="input-box">
                    <input type="email" name="email" value="<%= locals.old? old.email: '' %>" required>
                </div>
                <button type="submit">Confirm</button>
            </form>
        </div>
    </main>
    <footer>
        <p class="copyright color-white text-center">©Todos los derechos reservados a UniFi</p>
    </footer>
</body>
</html>