<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head') %>
    <link rel="stylesheet" href="/css/styles-login-register.css" />
    <script src="/js/metamaskLogin.js" type="text/javascript"></script>
  </head>
<body class="bg-dark-gray">
    <header>
        <nav class="header-navbar">
            <div class="logo">
                <a href="/"><img class="header-img" src="/img/logo.svg" alt="Logo de UniFi" /></a>
            </div>
        </nav>
    </header>
    <main class="main-container color-white">
        <div class="main-text">
            <h1>Create your UniFi account</h1>
            <p>Register with an email and password or just use your wallet</p>
        </div>
        <!-- Si se enviaron mensajes de error a la vista de registro, mostrarlos -->
        <% if (locals && locals.errorMessages) { %> 
            <% for(let error in errorMessages) { %>
            <p class="errors"><%= errorMessages[error].msg %> </p>
            <% } %> 
        <% } %> 

        <div class="login-form-container">
            <form class="login-form" method="POST" action="/register" enctype="multipart/form-data">
            <!-- Si existe la variable old (es decir el usuario se intentó registrar y la validación lo rechazó)
            colocarlo como valor por defecto del input para que quede disponible para editar -->
                <label for="email">Email</label>
                <div class="input-box">
                    <input type="email" name="email" value="<%= locals.old && old.email ? old.email : '' %>" required>
                </div>
                <label for="email">First Name</label>
                <div class="input-box">
                    <input type="first_name" name="first_name" value="<%= locals.old && old.first_name ? old.first_name : null %>">
                </div>
                <label for="email">Last Name</label>
                <div class="input-box">
                    <input type="last_name" name="last_name" value="<%= locals.old && old.last_name ? old.last_name : null %>">
                </div>
                <label for="email">Username</label>
                <div class="input-box">
                    <input type="user_name" name="user_name" value="<%= locals.old && old.user_name ? old.user_name : null %>">
                </div>
                <label for="password">Password</label>
                <div class="input-box">
                    <input  type="password" name="password" required>
                </div>

                <label for="avatar">Profile picture</label>
                <div>
                    <input class="file" type="file" name="avatar" required>
                </div>

                <button type="submit" alt="Register button">Register</button>
                <div class="other-links">
                    <a class="no-link" href="/login">Already have an account? Login</a>
                </div>
                
            </form>
            <div class="wallet-login">
                <img src="/img/metamask.svg" alt="Metamask Logo">
                <span id="addressText" class="text-center"></span>
                <button type="submit" id="metamaskButton" alt="Connect Metamask Wallet Button">Connect Wallet</button>
                <form action="/register" method="POST" id="metamaskLoginForm">
                    <input type="text" name="address" id="address" class="input-box" readonly>
                </form>
            </div>
        </div>
        <img src="/img/metamask.svg" alt="Metamask Logo" id="metamaskLogo" style="height: 60px; width: 100%; margin-top:30px">
    </main>
    <footer>
        <p class="copyright color-white text-center">©All rights reserved for UniFi</p>
    </footer>
</body>
</html>