<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head') %>
    <link rel="stylesheet" href="/css/styles-login-register.css" />
    <script src="/js/metamaskLogin.js" type="text/javascript"></script>
  </head>
<body class="bg-dark-gray">
    <header>
        <nav class="header-navbar">
            <div class="logo">
                <a href="/"><img class="header-img" src="/img/logo.svg" alt="Logo de UniFi" /></a>
            </div>
        </nav>
    </header>
    <main class="main-container color-white">
        <div class="main-text">
            <h1>Log into your UniFi account</h1>
            <p>Hi there, welcome back! Log in with your email and password or just use your wallet</p>
        </div>
        <!-- Si se enviaron mensajes de error a la vista de login, mostrarlos -->
        <% if (locals && locals.errorMessages) { %> 
            <% for(let error in errorMessages) { %>
            <p class="errors"><%= errorMessages[error].msg %> </p>
            <% } %> 
        <% } %> 

                <div class="errores">
                    <ul></ul>
                </div>


        <div class="login-form-container">
            <form class="login-form" action="/login" method="POST">

                <label for="email">Email</label>
                <div class="input-box">
                    <input class="email" type="email" name="email" value="<%= locals.old? old.email: '' %>">
                </div>

                <label for="password">Password</label>
                <div class="input-box">
                    <input class="password" type="password" name="password">
                </div>

                <label for="remember">
                    <input type="checkbox" class="checkbox" name="remember"> Remember Me
                </label>

                <button class="button" type="submit">Log in</button>

                <div class="other-links">
                    <a class="no-link" href="/reset-password">Forgot your password?</a>
                    <a class="no-link" href="/register">Don't have an account yet? Register</a>
                </div>

            </form>
            <div class="wallet-login">
                <img src="/img/metamask.svg" alt="Metamask Logo">
                <span id="addressText" class="text-center"></span>
                <button id="metamaskButton">Connect Wallet</button>
                <form action="/login" method="POST" id="metamaskLoginForm">
                    <input type="text" name="address" id="address" class="input-box" readonly>
                </form>
            </div>
        </div>
    </main>
    <footer>
        <p class="copyright color-white text-center">©Todos los derechos reservados a UniFi</p>
    </footer>
    <script src="/js/validationsLogin.js"></script>
</body>
</html>